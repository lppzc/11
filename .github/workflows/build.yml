# .github/workflows/build.yml
name: Build Windows

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4  # 升级到最新版本
    
    - name: Install Qt
      uses: jurplel/install-qt-action@v3
      with:
        version: 6.5.2  # 匹配CMakeLists.txt中的Qt6版本
        host: windows
        target: desktop
        arch: win64_msvc2019_64
        
    - name: Configure CMake
      run: |
        mkdir -p build
        cd build
        cmake .. -DCMAKE_BUILD_TYPE=Release
      shell: bash  # 使用bash避免路径问题
      
    - name: Build project
      run: |
        cd build
        cmake --build . --config Release
      shell: bash
      
    - name: Create distribution package
      run: |
        mkdir -p dist
        cp build/Release/TodoListGUI.exe dist/
        windeployqt --release dist/TodoListGUI.exe
      shell: bash
      
    - name: Upload artifact
      uses: actions/upload-artifact@v4  # 升级到最新版本
      with:
        name: todolist-windows
        path: build/Release/TodoListGUI.exe
        
    - name: Upload distribution package
      uses: actions/upload-artifact@v4
      with:
        name: todolist-windows-dist
        path: dist
